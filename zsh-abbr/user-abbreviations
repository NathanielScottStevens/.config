abbr "as"="alias | grep "
abbr "dburn"="docker stop \$(docker ps -aq); docker rm \$(docker ps -aq)"
abbr "dc"="docker-compose"
abbr "dcd"="docker-compose down"
abbr "dcu"="docker-compose up"
abbr "dirr"="direnv reload"
abbr "dk"="docker kill"
abbr "dps"="docker ps"
abbr "es"="env | grep -i "
abbr "gc"="git commit -m"
abbr "gcoa"="git checkout ."
abbr "gcos"="git for-each-ref --format='%(refname:short)' refs/heads | fzf | xargs git checkout"
abbr "glast"="git log -1 --format=%H | pbcopy"
abbr "glogt"="glod | grep 'Nathaniel\|tag' | less"
abbr "gs"="git status"
abbr "gtn"="grep '^\s\+\d\+)'"
abbr "gtodo"="git diff develop -S TODO"
abbr "mcc"="mix coveralls.html | grep -v '0$'"
abbr "mdg"="mix deps.get"
abbr "megm"="mix ecto.gen.migration"
abbr "mem"="mix ecto.migrate"
abbr "mer"="mix ecto.rollback -n=1"
abbr "mgc"="mix xref callers"
abbr "mgr"="mix xref graph --only-nodes --source"
abbr "mpgj"="mix phx.gen.json"
abbr "mpi"="iex -S mix phx.server"
abbr "mps"="mix phx.server"
abbr "mr"="iex -S mix run --no-halt"
abbr "mt"="mix test.watch"
abbr "mtn"="mix test | gtn"
abbr "po"="lsof -i -P -n | grep LISTEN"
abbr "s"="source ~/.zshrc"
abbr "ys"="yarn start"
abbr "yt"="yarn test"
